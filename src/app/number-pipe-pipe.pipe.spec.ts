import { NumberPipePipePipe } from './number-pipe-pipe.pipe';

describe('NumberPipePipePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberPipePipePipe();
    expect(pipe).toBeTruthy();
  });
});
